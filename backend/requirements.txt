# ==============================================================================
# Farmket Backend - Production Dependencies
# ==============================================================================
# Codebase-driven, direct runtime dependencies audited for Django, DRF, Channels,
# Celery, PostgreSQL, Redis, and JWT authentication.
# ==============================================================================

# Core Framework & ASGI Server
Django==6.0.4
daphne==4.2.3

# Django REST Framework & API Tools
djangorestframework==3.17.1
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.9.0
django-filter==26.1
drf-spectacular==0.30.0

# Database Driver (PostgreSQL)
psycopg2-binary==2.9.12

# Real-Time WebSocket Communication (Django Channels)
channels==4.3.2
channels-redis==4.3.0

# Asynchronous Tasks & Periodic Scheduling (Celery & Redis)
celery==5.6.3
django-celery-beat==2.9.0
redis==8.0.0

# Media & Image Handling
Pillow==12.2.0

# Environment Configuration
python-dotenv==1.2.2
